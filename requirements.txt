# Core — the pipeline runs end-to-end (dry-run) with just these.
anthropic>=0.49.0
PyYAML>=6.0

# Optional providers (uncomment / install to enable real backends):
#
#   YouTube publishing
# google-api-python-client>=2.0.0
# google-auth-oauthlib>=1.0.0
# google-auth-httplib2>=0.2.0
#
#   HTTP-based TTS / stock providers
# requests>=2.31.0
#
# ffmpeg must be available on PATH for real video rendering:
#   macOS:   brew install ffmpeg
#   Ubuntu:  apt-get install ffmpeg
